# Direct production dependency pins used by both container images.
#
# Regenerate deliberately after compatibility tests; application development
# dependencies remain flexible in pyproject.toml.
boto3==1.43.40
chronos-forecasting==2.3.1
peft==0.19.1
pandas==2.2.3
pyarrow==24.0.0
requests==2.32.3
streamlit==1.58.0
torch==2.8.0
